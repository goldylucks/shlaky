const SERVER = 'server'
const CLIENT = 'client'
const BOTH = 'both'
const CLI = 'cli'

const sidesConstants = {
  SERVER,
  CLIENT,
  BOTH,
  CLI,
  ALL: [SERVER, CLIENT, BOTH, CLI],
}

export default sidesConstants
